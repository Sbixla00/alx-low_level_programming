#include <stdio.h>
/**
 * main - Entry point
 *
 * Descreption : A C program that prints with put function
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	int i = 0;

	for (i = 122; i > 96; i--)
	{
	putchar(i);
	}
	putchar('\n');
	return (0);
}
